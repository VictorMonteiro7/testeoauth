import styled from "styled-components";

export const Container = styled.main`
  min-height: 100vh;
  min-width: 100vw;
  background: linear-gradient(to right, var(--purple) 70%, var(--white) 70%);
  color: var(--white);
`;
